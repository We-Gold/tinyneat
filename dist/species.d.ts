import { Config } from "./config";
import { Genome } from "./genome";
/**
 * Speciate the current population. Species are stored as arrays, where the first
 * element is the representative
 */
export declare const speciatePopulation: (population: Genome[], species: Genome[][], config: Config) => void;
//# sourceMappingURL=species.d.ts.map